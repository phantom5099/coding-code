# 上下文管理与压缩

上下文管理是独立域，不耦合在 Agent 内部。本文档介绍自动压缩机制、压缩策略和手动触发方式。

---

- **自动压缩**: 当上下文超过 token 预算的 90%（默认 200K），自动触发压缩
- **两种策略**:
  1. 截断旧工具输出
  2. 总结最早的消息
- **显式压缩**: 可通过 `POST /api/sessions/:id/compact` 手动触发
